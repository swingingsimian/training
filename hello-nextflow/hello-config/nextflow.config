// All of these can be over-ridden at runtime https://www.nextflow.io/docs/latest/config.html
docker.fixOwnership = true
// docker.enabled = true // is this the default? No, we don't need to explicitly set it to false
// omitting is results in it being run via bash locally
conda.enabled = true
